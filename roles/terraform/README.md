# terraform

Terraform, from the HashiCorp apt repository.

HashiCorp is the worst codename laggard of the repositories used here, often
months behind a new Ubuntu release, so this is where the
[apt_repo](apt_repo.md) suite probe and its fallback get real use.
