---
galaxy_info:
  role_name: secrets
  namespace: dotfiles
  author: irish1986
  description: Secrets from Infisical or local files, written to a shell cache in RAM and to project .env files.
  license: MIT
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - noble
# Always empty. Role dependencies bypass dotfiles_roles selection and execute silently; ordering is expressed once, in main.yml.
dependencies: []
