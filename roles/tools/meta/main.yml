---
galaxy_info:
  role_name: tools
  namespace: dotfiles
  author: irish1986
  description: Installs the tool entries listed in the profiles, their config files, and the removal list.
  license: MIT
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - noble
# Always empty. Role dependencies bypass dotfiles_roles selection and execute silently; ordering is expressed once, in main.yml.
dependencies: []
