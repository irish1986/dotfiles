---
vscode_packages:
  - code

vscode_dependencies:
  - apt-transport-https
  - ca-certificates
  - curl

# Microsoft publishes one release-independent "stable" suite.
vscode_apt_uri: https://packages.microsoft.com/repos/code
vscode_apt_key_url: https://packages.microsoft.com/keys/microsoft.asc
vscode_apt_suite: stable
vscode_apt_components:
  - main
