---
# The distro interpreter and its build headers, nothing more. uv, the interpreters uv manages and every Python tool this repo installs belong to the uv role; pipx is deliberately absent from both.
python_packages:
  - libssl-dev
  - python3
  - python3-pip
  - python3-venv
