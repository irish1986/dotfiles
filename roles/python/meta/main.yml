---
galaxy_info:
  role_name: python
  namespace: dotfiles
  author: irish1986
  description: The distro Python toolchain.
  license: MIT
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
# Always empty. Role dependencies bypass dotfiles_roles selection and execute silently; ordering is expressed once, in that list.
dependencies: []
