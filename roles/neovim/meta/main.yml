---
galaxy_info:
  role_name: neovim
  namespace: dotfiles
  author: irish1986
  description: Neovim and its Lua configuration.
  license: MIT
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
# Always empty. Role dependencies bypass dotfiles_roles selection and execute
# silently; ordering is expressed once, in that list.
dependencies: []
