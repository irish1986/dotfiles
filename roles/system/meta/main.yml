---
galaxy_info:
  role_name: system
  namespace: dotfiles
  author: irish1986
  description: Hostname, base packages and the guest agent.
  license: MIT
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
# Always empty. Role dependencies bypass dotfiles_roles selection and execute silently; ordering is expressed once, in that list.
dependencies: []
