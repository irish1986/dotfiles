---
zsh_binary: zsh
zsh_env_file: "{{ dotfiles_home }}/.zshenv"

# eza is only in the archive from 24.04; on jammy the zsh-eza plugin has no
# binary to wrap. Overridden in vars/Ubuntu-22.yml.
zsh_optional_packages:
  - eza
