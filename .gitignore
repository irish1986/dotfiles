.venv/
/.wip
.ansible
.env
.cache_ggshield

# Machine identity, seeded from docs/examples/group_vars-all.yml. Ignore the
# *contents* of group_vars rather than the directory: git never descends into
# an excluded directory, so a negation for a file inside one is never applied.
/inventory/group_vars/*
